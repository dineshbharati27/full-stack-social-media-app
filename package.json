{
  "name": "social-media-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "install": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run install && npm run build --prefix frontend && cp -r frontend/dist backend/dist",
    "start": "npm run build && node backend/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
